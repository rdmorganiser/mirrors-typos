[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rdmorganiser-mirrors-typos"
version = "0.0.1"
description = "Packaging shim for the typos pre-commit mirror"
license = "Apache-2.0"
requires-python = ">=3.8"
dependencies = [
    "typos==1.49.1",
]

[tool.setuptools]
packages = []
